pipeline {
    agent {
  label 'mylinux'
}

    stages {
        stage('install to maven') {
            steps {
                sh 'sudo dnf install maven -y'
            }
        }
    stage('clean package') {
            steps {
                sh 'mvn clean package'
            }
        }
    }
}
